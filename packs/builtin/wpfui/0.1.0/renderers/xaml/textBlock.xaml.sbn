<ui:TextBlock Text="{{text}}" Appearance="{{appearance}}" FontFamily="{{fontFamily}}" FontSize="{{fontSize}}" FontWeight="{{fontWeight}}" Foreground="{{foreground}}" TextWrapping="{{textWrapping}}" TextAlignment="{{textAlignment}}" Margin="{{margin}}" />

<ScrollViewer HorizontalScrollBarVisibility="{{horizontalScrollBarVisibility}}" VerticalScrollBarVisibility="{{verticalScrollBarVisibility}}" CanContentScroll="{{canContentScroll}}" Padding="{{padding}}" Margin="{{margin}}">{{slot.content}}</ScrollViewer>

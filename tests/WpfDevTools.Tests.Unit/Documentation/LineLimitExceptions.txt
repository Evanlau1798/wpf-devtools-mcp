# Explicit temporary exceptions for the 500-line source/test/script policy.
# Format: repository-relative path | reason
.github/workflows/ci-cd.yml | Temporary workflow orchestration exception; split into reusable workflow/actions before removing.
.github/workflows/release.yml | Temporary release workflow orchestration exception; split staging/signing/upload lanes before removing.
scripts/online-installer.ps1 | Temporary installer entrypoint exception; release asset discovery and archive trust helpers are split into scripts/installer/.
scripts/tools/packaging/Write-ReleaseSidecars.ps1 | Temporary release sidecar/SBOM generation exception; split SBOM document generation into a helper module before removing.

<Border Padding="{{padding}}" Margin="{{margin}}" BorderThickness="{{borderThickness}}" CornerRadius="{{cornerRadius}}" Background="{{background}}" BorderBrush="{{borderBrush}}" MaxWidth="{{maxWidth}}" MinHeight="{{minHeight}}">{{slot.content}}</Border>

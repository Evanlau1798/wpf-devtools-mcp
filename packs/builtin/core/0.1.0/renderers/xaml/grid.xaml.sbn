<Grid Margin="{{margin}}" HorizontalAlignment="{{horizontalAlignment}}" VerticalAlignment="{{verticalAlignment}}">
  <Grid.RowDefinitions>{{slot.rows}}</Grid.RowDefinitions>
  <Grid.ColumnDefinitions>{{slot.columns}}</Grid.ColumnDefinitions>
  {{slot.children}}
</Grid>

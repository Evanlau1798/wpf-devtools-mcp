<ui:Button Content="{{text}}" Appearance="{{appearance}}" Command="{{command}}" Margin="{{margin}}" MinWidth="{{minWidth}}" HorizontalAlignment="{{horizontalAlignment}}">
  <ui:Button.Icon>{{slot.icon}}</ui:Button.Icon>
</ui:Button>

{
  "schemaVersion": "wpfdevtools.ui-block.v1",
  "kind": "wpfui.navigationView",
  "displayName": "Navigation View",
  "description": "Semantic WPF UI Navigation View block.",
  "category": "navigation",
  "properties": {
    "paneDisplayMode": {
      "description": "Navigation pane placement for this persistent desktop shell. Left uses the standard item template. LeftMinimal uses the compact item template; combine it with isPaneOpen=false for a narrow icon rail. LeftFluent uses large icons with titles underneath. Top and Bottom place navigation on the matching horizontal edge.",
      "default": "Left",
      "enum": ["Left", "LeftMinimal", "LeftFluent", "Top", "Bottom"],
      "required": false,
      "type": "string"
    },
    "isPaneOpen": {
      "description": "Whether the left navigation pane is expanded. Set false with LeftMinimal for a narrow icon rail; provide icon slots so collapsed entries remain recognizable.",
      "default": true,
      "required": false,
      "type": "boolean"
    }
  },
  "slots": {
    "content": {
      "description": "Primary page content displayed in the navigation content surface.",
      "allowedKinds": [
        "*"
      ]
    },
    "footer": {
      "description": "Persistent navigation actions rendered at the bottom of the pane.",
      "allowedKinds": [
        "wpfui.navigationViewItem",
        "wpfui.button"
      ]
    },
    "items": {
      "description": "Ordered primary navigation entries and separators.",
      "allowedKinds": [
        "wpfui.navigationViewItem",
        "wpfui.navigationViewItemSeparator"
      ]
    }
  },
  "renderer": {
    "xamlTemplate": "renderers/xaml/navigationView.xaml.sbn"
  },
  "validation": {},
  "sourceHints": [
    {
      "path": "src/Wpf.Ui/Controls/NavigationView/NavigationView.Base.cs",
      "usage": "semantic extraction"
    }
  ]
}

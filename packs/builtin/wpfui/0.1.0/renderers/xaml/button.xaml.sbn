<ui:Button Content="{{text}}" Appearance="{{appearance}}" Command="{{command}}" Margin="{{margin}}" MinWidth="{{minWidth}}" HorizontalAlignment="{{horizontalAlignment}}"{{?property.fontSize}} FontSize="{{fontSize}}"{{/property.fontSize}}>
  <ui:Button.Icon>{{slot.icon}}</ui:Button.Icon>
</ui:Button>

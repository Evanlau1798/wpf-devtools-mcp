<ui:TextBlock Text="{{text}}" Appearance="{{appearance}}" FontFamily="{{fontFamily}}" FontSize="{{fontSize}}" FontWeight="{{fontWeight}}" TextWrapping="{{textWrapping}}" TextAlignment="{{textAlignment}}" Margin="{{margin}}" />

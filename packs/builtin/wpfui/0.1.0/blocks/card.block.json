{
  "schemaVersion": "wpfdevtools.ui-block.v1",
  "kind": "wpfui.card",
  "displayName": "Card",
  "description": "Semantic WPF UI Card block. For a dense repeated shelf, define an explicit content budget for media, title, metadata, and padding; avoid a minHeight or generous padding that leaves unexplained empty space. Keep selected and unselected siblings on identical outer geometry; allocate the selected border and padding inside that fixed footprint so selection does not change the row baseline or item size.",
  "category": "container",
  "properties": {
    "horizontalContentAlignment": {
      "description": "Horizontal alignment applied to the Card's header and content stack.",
      "default": "Stretch",
      "enum": [
        "Left",
        "Center",
        "Right",
        "Stretch"
      ],
      "type": "string"
    },
    "margin": {
      "description": "Outer spacing around the Card, using WPF Thickness syntax.",
      "default": "0",
      "format": "thickness",
      "type": "string"
    },
    "maxWidth": {
      "description": "Maximum rendered width of the Card in device-independent pixels.",
      "default": 1000000,
      "minimum": 0,
      "type": "number"
    },
    "minHeight": {
      "description": "Minimum rendered height of the Card in device-independent pixels.",
      "default": 0,
      "minimum": 0,
      "type": "number"
    },
    "padding": {
      "description": "Inner spacing around the Card's header and content, using WPF Thickness syntax.",
      "default": "16",
      "format": "thickness",
      "type": "string"
    },
    "verticalContentAlignment": {
      "description": "Vertical alignment applied to the Card's header and content stack.",
      "default": "Stretch",
      "enum": [
        "Top",
        "Center",
        "Bottom",
        "Stretch"
      ],
      "type": "string"
    }
  },
  "slots": {
    "actions": {
      "description": "Action buttons rendered horizontally in the Card footer.",
      "allowedKinds": [
        "wpfui.button"
      ]
    },
    "content": {
      "description": "Primary Card body rendered after the optional header.",
      "allowedKinds": [
        "*"
      ]
    },
    "header": {
      "description": "Optional heading content rendered before the Card body.",
      "allowedKinds": [
        "core.text",
        "wpfui.textBlock"
      ]
    }
  },
  "renderer": {
    "xamlTemplate": "renderers/xaml/card.xaml.sbn"
  },
  "validation": {},
  "sourceHints": [
    {
      "path": "src/Wpf.Ui/Controls/Card/Card.cs",
      "usage": "semantic extraction"
    }
  ]
}
